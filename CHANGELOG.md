# CHANGELOG — SoundLog (한바다 작업일지)

변경사항은 배포할 때마다 이 파일에 기록합니다.

---

## 🔴 빠른 진입 (한동안 안 했던 사람)

**지금 뭘 하나?**
- 📌 **현재 버전**: v0705-03 (2026-07-05)
- 🌐 **라이브 상태**: 정상
- ✅ **배포 방식**: GitHub Pages + GitHub Actions (자동)

**최근 3개 배포**:
1. **(배포 예정)** 장비 수정저장 무반응 근본 수정(`localStorage.setItem` catch 없는 try 안에 있던 문제) + `persistEquipment()`/`migrateOldEquipPhotos()` 추가
2. **v0705-03 + `e399b61`** (2026-07-05) — 장비 저장 팝업 반응 지연 근본 수정(사진 업로드도 로컬 반영 뒤로) + 오래된 base64 사진 백그라운드 정리
3. **v0705-02** (2026-07-05) — 행사일지 목록 지연 표시 수정: 조회 모드에서도 백그라운드 재조회 허용

**최근 해결된 문제**:
- ✅ 장비 수정저장 눌러도 팝업 안 닫히고 반응 없던 문제 — `equipmentData` 저장 코드 13곳이 catch 없는 try 안에 있어 예외 시 조용히 멈추던 것이 진짜 원인. `persistEquipment()`로 통일해 예외 흡수 + `migrateOldEquipPhotos()`로 오래된 base64 장비 사진도 백그라운드 정리
- ✅ 장비 저장 팝업 반응 지연 — v0705-03에서 놓친 `_migrateEquipPhotos`(사진 업로드)까지 로컬 반영 뒤로 이동
- ✅ 오래된 행사일지에 남은 base64 사진을 앱 시작 5초 후 백그라운드에서 Storage URL로 자동 정리(migrateOldLogPhotos)
- ✅ 장비 저장 팝업이 클라우드 응답을 기다리느라 안 닫히던 문제 — 로컬 반영 즉시 닫기 + 20초 타임아웃
- ✅ 행사일지 목록 화면 진입 시 다른 기기의 최신 항목이 몇 초간 안 보이던 문제
- ✅ 행사일지 삭제·저장 관련 연쇄 버그(v0703-62~73) 근본 수정 — 상세는 아래 섹션 참고
- ✅ 행사일지 저장 메시지 미표시 — updateTitle() 무방비 예외로 saveAndSync 조기 중단되던 진짜 원인 수정 + iOS 캐싱 방지
- ✅ 음향 장비 스피커브랜드 5개 모델로 세분화

**다음 예정**:
- 🛠️ iOS 홈스크린 캐시 무효화(서비스워커 도입 검토) — 감사실장 지적: 수정해도 재발하는 사례의 유력 원인
- 🛠️ 추가 기능 개발 (사장 지시 예정)

**더 알아보기**:
- 📖 전체 가이드: [README.md](README.md) 읽기 (5분)
- 🔧 현재 상태: 아래 최신 배포 로그 참조
- 🐛 버그/제안: 사장님께 보고

---

## [2026-07-05] (배포 예정) — 장비 저장 팝업 반응 지연 근본 수정 + base64 사진 정리

- `e399b61` v0705-03에서 사진 업로드(`_migrateEquipPhotos`)를 여전히 로컬 반영보다 먼저 await 하던
  잔여 지연 수정 (`saveEquipEdit`/`saveEquipAdd`, index.html). 로컬 저장·화면 갱신·팝업 닫힘을 먼저
  실행하고, 사진 업로드→클라우드 동기화를 20초 타임아웃 안에서 순서대로 처리.
- `e399b61` `migrateOldLogPhotos()` 추가: 앱 로딩 5초 후 백그라운드로 실행되어 base64가 남은
  옛 행사일지 사진을 Storage URL로 교체하고 클라우드에도 반영. base64 없는 로그는 건드리지 않음.

---

## [2026-07-05] v0705-01~03 — 목록 지연 표시 + 장비 저장 팝업 반응성 수정

- **v0705-03** `615d32b` 장비 수정/추가 저장 시 팝업 즉시 닫힘 + 화면 즉각 반영 수정 (클라우드 동기화 20초 타임아웃, 로컬 반영 우선)
- **v0705-02** `03cbc1e` 행사일지 목록 지연 표시 수정: 조회 모드에서도 백그라운드 재조회 허용
- **v0705-01** `267a30e` 문서 체계 정비: 버전 정보 동기화 + PROJECT_LOG 온보딩 추가 + 레거시 인사말 훅 제거

---

## [2026-07-03] v0703-74~75 — 배포 재시도 (코드 변경 없음)

- GitHub Pages 배포 일시 오류로 인한 워크플로 재실행. index.html 실질 코드 변경 없음.

---

## [2026-07-03] v0703-62~73 — 행사일지 저장/삭제 연쇄 버그 근본 수정

- **v0703-73** `ebef06b` 배포 화면 버전 번호가 항상 하나 뒤처지던 근본 원인 수정: 버전 증가를 배포 전으로 이동
- **v0703-72** `827eb10` 행사일지 삭제 로직 재설계: 서버 삭제 확인 후에만 화면에서 제거
- **v0703-71** `f497d96` 행사일지 삭제 재발 근본 수정 + 다른 카테고리와 동일한 삭제 암호 확인 추가
- **v0703-70** `7db6b6e` 행사일지 삭제 후 되살아남 수정: 실시간 저장 에코가 삭제 직후 항목을 복원하던 경쟁 조건 제거
- **v0703-69** `93f0127` 행사일지 저장 무반응 수정: 클라우드 응답 대기 20초 제한 + 단계별 오류 방어
- **v0703-68** `b51133b` 행사일지 삭제 즉시 화면 반영: 클라우드 대기 없이 목록에서 바로 제거
- **v0703-67** `e2fd11a` 토스트 메시지 화면 중앙 표시 + 저장 완료 메시지 덮어쓰기 수정
- **v0703-66** `1aac864` 긴급 버그 수정: 신규 일지 작성 중 데이터 유실 + 실시간 전파 누락 2건
- **v0703-65** `889dd46` CRUD 전면 점검: 거짓 성공 메시지 수정 + 클라우드 동기화 확대

---

## [2026-07-03] v0703-61 — 행사일지 저장 메시지 미표시, 진짜 근본 원인 수정 (감사 후 재진단)

상세 원인·재발방지 체크리스트는 PROJECT_LOG.md 참고 (v0703-56~60 4회 오진 → 감사실장 재진단으로 진짜 원인 확인, updateTitle() 예외 처리 + 캐시 방지 메타태그로 수정).

---

## [2026-07-03] v0703-60 — 행사일지 저장 메시지 안내 버그 수정

### 문제
- 저장 버튼 클릭 후 "저장완료" 메시지가 보이지 않음 (데이터는 정상 저장됨)
- 원인: `showToast()` 함수가 메시지를 3.5초 후 자동으로 사라지게 했으나, 사진 업로드와 Supabase 동기화가 3.5초 이상 소요되면서 메시지 표시 시간 초과

### 수정
- **showToast() 함수 개선** (6760-6767줄):
  - `clearTimeout()` 추가로 타이머 충돌 방지
  - `duration` 파라미터 추가 — 메시지별 표시 시간 커스터마이징 가능
  - 전역 `toastTimeout` 변수로 타이머 관리

- **saveAndSync() 함수** (6839줄):
  - "저장 중..." 메시지 표시 시간을 **5초(5000ms)로 연장**
  - 사진 업로드 및 클라우드 동기화 완료까지 메시지 표시 유지

---

## [2026-07-03] v0703-55 — 음향장비 스피커브랜드 세분화 + 다중 선택 지원

### 변경
- **스피커브랜드 항목 세분화** (4개 → 5개 모델 + 기타)
  - D&B array (V8+V-sub)
  - D&B stack (V8+V-sub)
  - JBL 625
  - JBL 815+818s
  - LDsystems MAUI44G2
  - 기타 (이유 입력 필드)

- **다중 선택 지원**: 여러 스피커 브랜드 동시 선택 가능 (기존: 1개만)
- **기타 입력 필드**: 기타 선택 시 자동 표시 → 선택 이유 기록
- **데이터 호환성**: 레거시 데이터 자동 변환 (구 data-val → 신 data-val)

---

## [2026-06-25] v0622-54 — 장비관리 1개만 보이던 문제 근본 수정 + 중복 379개 정리

### 원인
- 기본 장비를 만들 때 카테고리(`cat`)를 안 넣어 저장 → 불러올 땐 `cat` 없는 항목을 버림 → 화면엔 안 보이고, 비었다고 판단해 매번 새 ID로 다시 생성 → 클라우드에 카테고리 빈 행이 380개까지 누적, 화면엔 카테고리가 있는 1개(어제 추가분)만 표시됨.

### 수정
- `initEquipmentDefault`: 기본 장비에 **카테고리(`cat`) 포함** + **고정 ID(`eq_def_<cat>_<name>`)** 사용 → 여러 기기·여러 번 로드돼도 같은 행에 덮어써져 **중복 누적 차단**.

### 데이터 정리 (1회 작업)
- 원본 단일행(`equipment`)의 실데이터 63개 + 어제 추가분(LD) 1개 = **64개를 카테고리 붙여 `eq_` 행으로 재정비** (음향36·무대17·특효6·기타5).
- 카테고리가 빈 **쓰레기 행 379개 삭제**. 원본 단일행은 백업으로 보존.

---

## [2026-06-24] v0622-53 — 장비 사진 갤러리 선택 허용

### 수정
- 장비 사진 업로드 시 카메라만 열리던 문제 해결 (`capture="environment"` 제거)
- 이제 폰에서 탭하면 "사진 보관함 / 카메라" 선택 가능

---

## [2026-06-24] v0622-52 — 장비관리 데이터 구조 변경: 단일 JSON 행 → 장비별 개별 행

### 변경 (근본 수정)
- 장비 전체를 JSON 덩어리 1행에 저장하던 구조 → **장비 1개 = `vehicle_data` 1행** (`id='eq_{item.id}'`)
- PATCH 페이로드 수십KB↑ → 수백bytes로 축소 → 저장 실패·Realtime 누락 해소
- Realtime: 특정 장비 변경 시 해당 장비만 반영 (기기 간 자동 동기화)
- 기존 `id='equipment'` 단일 행 → 앱 로드 시 자동 마이그레이션 후 삭제
- `_syncEquipmentCloud(item)` 개별 저장, `_deleteEquipCloud(itemId)` 개별 삭제 신설
- 행사일지와 동일한 1레코드=1행 구조로 통일

---

## [2026-06-24] v0622-51 — 장비관리 ☁️ 수동 불러오기 버튼 추가

### 추가
- 장비관리대장 상단에 ☁️ 버튼 추가 — 클라우드에서 장비 데이터 수동 재로드
- `manualLoadEquipment()` 함수 신설

---

## [2026-06-24] v0622-50 — 장비관리 상태 필터 바 제거 (UI 단순화)

### 변경
- 전체/사용중/미사용 필터 바 제거 — 폰에서 화면 복잡해 보이는 문제 해소
- `setEqFilter`, `eqCurrentFilter` 관련 코드 전체 제거

---

## [2026-06-24] v0622-49 — 장비 추가/수정 이중 제출 방지

### 수정
- `saveEquipAdd` / `saveEquipEdit` 이중 제출 방지: `_eqSaving` 플래그로 진행 중 재호출 차단 (저장 완료 전 버튼 두 번 탭 시 중복 추가 방지)

---

## [2026-06-23] v0622-48 — 장비관리 데이터 소실·동기화 실패 근본 수정

### 수정 (저장 사라짐 / 기기 간 동기화 안 됨 / 화면 이상)
- **장비 사진 base64 → Supabase Storage URL 전환** (핵심 원인)
  - 기존: 사진을 base64로 `equipmentData`에 넣고 전체를 `vehicle_data.v15` 한 칸에 통째 업로드
    → 행이 비대해져 저장 실패/Realtime 누락 → 소실·동기화 실패
  - 신규 `uploadEquipPhoto()` — `event-photos` 버킷 `equipment/{id}/0.jpg` 경로 (행사일지와 동일 패턴)
  - 사진 선택 시 즉시 업로드, 실패 시 base64 폴백
  - 저장 시 남은 base64 자동 마이그레이션(`_migrateEquipPhotos`), 삭제 시 Storage 파일도 제거
- **일시 로드 실패 시 기본값 덮어쓰기 방지**: `initEquipmentDefault(cloudOk)` — 클라우드 로드 실패+로컬 데이터 존재 시 시드/sync 건너뜀(실데이터 보존)
- **동기화 실패 알림**: `_syncEquipmentCloud` 성공/실패 반환, 저장 시 실패하면 "⚠ 로컬 저장됨 · 클라우드 실패" 토스트
- 탭 진입 시 `switchEqCat`로 카테고리/소분류 바 상태 정합

---

## [2026-06-23] v0622-47 — 장비관리 토스트 수정 + 필드명 변경

### 수정
- 장비 추가/수정 저장 후 토스트 메시지 안 보이던 버그 수정
  - `showToast` 먼저 호출 → 300ms 후 모달 닫기 (toast가 모달 뒤에 가리던 타이밍 문제 해소)
- 폼·뷰 라벨 변경: **장비명 → 모델명**, **모델명 → 제조회사**
- 카드: 모델명(큰 글씨 15px), 제조회사(작은 글씨 11px 아래)
- 검색 플레이스홀더: "장비명 / 모델 검색…" → "모델명 / 제조회사 검색…"

---

## [2026-06-23] v0622-46 — 장비 추가 토스트 메시지 안 보이는 버그 수정

### 수정
- 장비 추가/수정 후 "✅ 장비 추가 완료" 토스트가 안 보이던 문제 수정
  - 원인: 토스트 `z-index:999` < 장비 모달 `z-index:3000` → 모달 뒤에 가려짐
  - 수정: 토스트 `z-index` → `99999` (모든 모달 위)

---

## [2026-06-23] v0622-45 — 내보내기 속도 근본 해결 + 리프트 등 복잡 모델 오류 수정

### 수정
- **id 치환 O(N×텍스트) → O(텍스트) 1회 패스로 교체** (가장 큰 속도 원인)
  - 기존: id 개수만큼 정규식 3개씩 전체 텍스트 반복 (id 200개면 600번 스캔)
  - 개선: 패턴 1개로 전체 텍스트 1번 스캔 + Map 조회 → 파일 크기에만 비례
- **리프트 등 복잡 모델 오류 수정**: `visual_scene` 최상위 `<node>`가 여러 개인 모델에서
  첫 번째 노드만 잡던 정규식 제거 → 전체 scene 내용 그대로 사용

---

## [2026-06-23] v0622-44 — 내보내기 속도 대폭 개선 (모델 1회 처리 + 인스턴싱)

### 수정
- 객체가 많을 때 내보내기가 멈출 듯 느리던 근본 원인 해결
  - 기존: **객체마다** 모델 XML 전체 재파싱 + id 전체 치환(정규식 수십 번) 반복
    → 같은 모델 N개면 무거운 처리 N번 (객체 수에 비례해 폭증)
  - 수정: **고유 모델당 딱 1회만** 파싱·id치환 후 `library_nodes`에 등록,
    각 배치는 `instance_node` 참조로만 처리
    → 같은 모델 여러 개여도 처리 비용 1회 (재중심 매트릭스는 배치별 유지)

---

## [2026-06-23] v0622-43 — 등록 3D 모델 위치 어긋남 수정

### 수정
- 스케치업 내보내기한 .dae를 등록한 객체가 **터무니없는 위치**에 찍히던 버그 수정
  - 원인: 모델 내부 좌표가 원점(0,0,0)이 아닌데 스케일만 적용 → 내부 오프셋×스케일만큼 멀리 튕김
  - 수정: 트러스와 동일하게 **재중심(re-center) 매트릭스** 적용
    - bbox XY 중심 → 배치 지점, Z 최저점 → 바닥에 정렬
  - bbox 계산 실패 시에만 기존 스케일 방식으로 폴백

---

## [2026-06-23] v0622-42 — 도면 세팅 저장/불러오기

### 신규
- 무대 도면(3D) 탭에 **세팅 저장/불러오기** 기능 추가
  - 내보내기 전 현재 배치 + 무대 크기 + 높이를 이름 붙여 저장
  - 저장된 세팅 목록에서 📂 불러오기 → 즉시 복원 (테스트·재사용)
  - 같은 이름이면 덮어쓰기 확인, 🗑️ 삭제는 암호 2375
  - localStorage 저장 + Supabase `vehicle_data` (id=`dae_setups`) 클라우드 동기화
  - 3D 탭 열면 클라우드에서 세팅 목록 자동 로드 (기기 간 공유)

---

## [2026-06-23] v0622-41 — .dae 내보내기 속도 개선 (병렬 모델 로딩)

### 변경
- `.dae` 내보내기 시 등록된 3D 모델 파일을 **병렬(Promise.all)로 미리 fetch**
  - 기존: 객체마다 순차 fetch → 객체 수 × 네트워크 지연으로 멈추는 증상
  - 개선: 고유 URL 목록을 한꺼번에 fetch → 캐시에 저장 → 루프에서 캐시 사용
- `_embedDaeModel(url, prefix, prefetchedText)` 3번째 인자 추가 (캐시 텍스트 직접 전달)
- 내보내기 시작 시 "⏳ 3D 모델 N개 로딩 중..." 토스트 표시

---

## [2026-06-23] v0622-40 — 내보내기 등록모델 실제형상 복원

### 변경
- `.dae` 내보내기: 등록된 3D 모델이 있는 객체는 **실제 형상으로 내보냄** (트러스처럼)
  - 직전 버전에서 박스로만 나오던 문제 수정
- 등록 모델이 **없는** 트러스 외 객체만 1×1×1m 박스 위치 마커로 처리
- 무대 평면도(2D) 표시는 그대로 1×1m 균일 마커 유지 (위치 잡기용)

---

## [2026-06-23] v0622-39 — 평면도/내보내기 객체 1×1m 균일화

### 변경
- 무대 평면도에 표시되는 객체 크기를 모두 **1m × 1m로 균일화** (위치 마커)
- `.dae` 내보내기도 트러스 외 객체는 **1×1×1m 박스 마커**로 통일 (모델 임베드 건너뜀)
- **트러스는 기존대로** 실제 치수·조립 형상 유지
- 의도: 평면도에서는 위치만 잡고, 세부 모델·편집은 스케치업에서 진행

---

## [2026-06-23] v0622-38 — 등록현황 미등록(흰색) 항목 숨김 기능

### 변경
- 등록현황에서 ⬜(미등록) 기본 타입에도 🗑️ 버튼 추가 — 안 쓰는 항목을 목록에서 숨김 (암호 2375)
- 숨긴 타입은 `lib3dHiddenTypes`(localStorage)에 저장, 업로드 드롭다운에서도 제외
- 그룹 안 항목이 모두 숨겨지면 카테고리 헤더도 자동 생략
- 하단에 "↩ 숨긴 기본 항목 N개 복원" 버튼 추가

---

## [2026-06-23] v0622-37 — 평면도 흰 화면 버그 수정

### 수정
- 등록 부품을 평면도에 넣으면 흰 화면이 되는 버그 수정
  - 원인: SVG 텍스트 노드에 `&`, `<`, `>` 등 XML 특수문자가 들어가면 SVG 전체가 깨짐
  - `esc()` 함수 추가 — SVG 내 모든 텍스트 노드를 XML 이스케이프 처리
- 장비 치수(`rw/rd`)가 NaN이거나 0 이하이면 기본값(0.9m)으로 자동 보정
- 개별 장비 렌더링 오류가 나도 평면도 전체가 깨지지 않도록 try-catch 추가
- `front_mon`, `sidefill`, `delay`, `placard` EQ 코드 추가

---

## [2026-06-23] v0622-36 — 트러스 피커 쌍 차감 + 배치 팝업 등록부품 연동

### 트러스 설정 피커
- 쌍 설치 반영: + 한 번에 실제 2개 차감 (수치도 2, 4, 6… 으로 표시)
- 재고 도트가 쌍(×2)만큼 차감되도록 수정 — 남은 개수 정확히 표시
- 재고 부족 시 + 차단 (보유 수량 초과 방지, 토스트 안내)
- "재고 N개 / 남은 N개" 텍스트·"3D" 뱃지 제거 → 도트 그래픽으로 파악
- 조각 블록·버튼·도트 정렬 및 간격 정리 (큐브 고정 양끝 표시)

### 무대 평면도 배치 팝업
- 화면 클릭 시 뜨는 배치 팝업이 **3D 라이브러리에 등록된 부품 목록**으로 표시됨
- `daeMeta()` 추가 — 등록 부품(커스텀 포함)의 아이콘·이름을 배치 목록·라벨에 반영
- 등록 부품이 없으면 기본 장비 표시, 트러스·코너·관객석·전체삭제는 항상 제공

---

## [2026-06-23] v0622-35 — 등록현황 삭제 버튼 표시 버그 수정

### 수정
- 등록현황에서 **기존(내장) 타입에 업로드한 항목도 삭제** 가능하도록 수정
  - 원인: 4열 그리드에서 라벨 `min-width:auto` 때문에 삭제 버튼이 셀 밖으로 밀려 안 보였음
  - 라벨에 `min-width:0`, 버튼에 `flex-shrink:0` 적용 → 항상 표시
- 등록현황 2열 그리드로 변경 (버튼 공간 확보)
- 삭제 버튼 ✕ → 🗑️ 로 변경하고 크기 키움 (가시성·터치 영역 개선)
- 삭제 시 암호 2375 확인 (모델 삭제·커스텀 타입 삭제 모두)

---

## [2026-06-23] v0622-34 — 트러스 설정 비주얼 피커 + 라이브러리 UX 개선

### 변경
- 트러스 설정 팝업: 세로 목록 → 가로 비주얼 피커로 전면 개편
  - 조각 블록(색상·비례 너비)과 재고 도트 표시 (녹색=사용중, 파란색=여유, 회색=없음)
  - 조립 미리보기 행: + 누를 때마다 블록이 추가되고 재고 도트가 차감됨
  - 높이(elevation) 조절을 헤더 한 줄로 압축, 전체 팝업이 화면 안에 들어옴
- 3D 모델 라이브러리에 🎸 악기류 / 📦 그외 카테고리 추가
- 등록현황: 등록된 항목 전체에 ✕ 삭제 버튼 추가 (암호 2375)
  - 커스텀 타입: 미등록 상태에서도 ✕ 로 타입 자체 삭제 가능
  - 기존 내장 타입: 등록된 모델만 삭제 가능
- 편집 모달 삭제 버튼에 암호(2375) 추가
- 등록현황 행에 ✏️ 버튼 추가 → 편집 모달 바로 열기
- 하단 카드형 3열 그리드(lib3d-list) 제거 — 등록현황으로 통합
- 직접 타입 추가 카테고리에 악기류/그외 추가
- 수정 버튼(✏️) 작동 버그 수정 (modal-overlay show 클래스 누락)

---

## [2026-06-23] v0622-33 — 3D 모델 라이브러리 카드 컴팩트화 + 커스텀 타입 카테고리 + 중복방지

### 변경
- 모델 리스트를 2열 반응형 그리드로 변경 (가로 길이 단축, 카드 패딩·폰트 축소)
- 직접 타입 추가 시 카테고리 선택 (스피커류/콘솔·마이크/무대·구조물/트러스 부품)
  - 커스텀 타입이 선택한 카테고리 그룹 안에 표시됨 (등록현황·업로드 드롭다운·편집 모달 모두)
- 편집 모달에서 타입 변경 시 중복 방지: 대상 타입에 이미 모델이 있으면 교체 확인 후 기존 모델 삭제
- 업로드 드롭다운·등록현황·편집 모달의 커스텀 타입을 카테고리별로 그룹핑 (`data-cat` 기반)

---

## [2026-06-23] v0622-32 — 3D 모델 라이브러리 UX 개선 (컴팩트 카드 + 팝업 편집)

### 변경
- 모델 카드 컴팩트화: 치수/스케일/원점 인라인 입력 제거 → 아이콘·이름·타입·치수 요약만 표시
- 편집 팝업 신설 (`lib3dOpenEditModal`): 카드 클릭 또는 ✏️ 버튼 → 모달에서 이름·타입·W/D/H·스케일·원점 수정
- 타입 변경 기능: 편집 모달에서 드롭다운으로 카테고리 이동 가능
- 커스텀 타입 추가 후 드롭다운 자동 선택 → 바로 업로드 가능
- `lib3dBuildTypeOptions()` 헬퍼 함수 추가 (편집 모달용 타입 옵션 생성)
- `lib3dSaveEdit()` 함수 추가 (편집 내용 저장 + 클라우드 동기화)

---

## [2026-06-23] v0622-31 — 3D 모델 라이브러리 전체 너비 확장 및 정렬

### 변경
- 3D 도면 탭: 📍 행사장 위치 섹션 삭제 (미사용 기능 제거)
- 3D 모델 라이브러리를 전체 너비로 확장 (이전: 50% 분할 레이아웃)
- 등록 현황 그리드: 2열 → 4열로 확장 (라이브러리 항목 많아져도 한눈에 파악)
- 타입 드롭다운 순서 재정렬: 트러스 부품 → 스피커류 → 콘솔/마이크 → 무대/구조물

---

## [2026-06-22] v0622-30 — 🎯 트러스 .dae 모델 자동 정렬 (bbox 평탄화 + 슬롯 핏)

### 추가
- `_daeModelBBox()`: 모델 .dae의 전체 노드 트리(matrix·instance_node 체인)를 평탄화해
  실제 월드 바운딩박스(미터)를 계산 — 단위(inch/mm) 자동 보정
- `_trussFitMatrix()`: 모델 bbox를 트러스 슬롯에 중심 맞춰 끼우는 4x4 affine 생성
  - 길이축 자동 감지(최대 extent) → 슬롯 길이축(수평=X/수직=Z)으로 회전 정렬
  - 슬롯 크기에 맞춰 스케일 → 위치·방향·크기 자동 정합, 틈/겹침 제거

### 수정
- 트러스 조각 임베드: 기존 단순 translate/rotate → bbox 기반 fit matrix로 교체
  - SketchUp 모델의 비균일 스케일·0,0,0 미정렬·라벨≠실제길이 문제 모두 흡수

---

## [2026-06-22] v0622-29 — 🏗️ 트러스 무대 중앙 정렬 + 실제 .dae 조각 임베드 재활성화

### 수정
- **트러스 무대 중앙 정렬**: 평면도에서 어디 클릭하든 X=0(무대 가로 중심)에 자동 정렬
  - Y(앞뒤) 방향은 클릭 위치 그대로 반영
- **실제 .dae 조각 임베드 재활성화**: 3D 라이브러리에 등록된 트러스 조각(truss_0.5/1/1.5/2/cube) 있으면 박스 대신 실제 형상 사용
  - 0,0,0 기준으로 만든 .dae 한정 (흩어진 좌표 충돌 방지)
  - 수평 조각: Y축 90도 회전 적용(Z→X), 수직 조각: 회전 없음
  - 모델 미등록 시 기존 박스 폴백 유지

---

## [2026-06-22] v0622-28 — 📦 트러스 창고 재고 실제 보유수량 기본 시드

### 추가
- 트러스 재고 기본값: 0.5m·1m·1.5m ×4, 2m ×6, 큐브 ×8
- 재고 미설정/전부 0일 때만 자동 시드, 사용자가 입력한 값은 그대로 보존

---

## [2026-06-22] v0622-27 — 🔧 트러스 세로바 간격(틈) 수정

### 수정
- **세로 조각 틈 해결**: `boxGeom`은 Z가 바닥 기준인데 v25에서 세로 조각에 `+length/2`를
  더해 각 조각이 절반씩 떠 틈이 생기던 문제 수정
  - 세로조각 Z: `relZ + offset + length/2` → `relZ + offset` (바닥 기준 타일링)
  - 가로조각 X는 중심 기준이라 `+length/2` 유지 (정상)
- **상·하 가로바 Z 정렬**: 하단바 `cubeH/2→0`(프레임 바닥), 상단바 `vSpan-cubeH/2→vSpan-cubeH`
  - 세로바 상단이 상단 가로바 하단과 정확히 맞물림, 하단바는 무대면에 착지

---

## [2026-06-22] v0622-26 — 🏗️ 트러스 무대 착지 + 재고 제약 + 코드 정리

### 수정
- **공중 부양 해결**: 트러스 프레임이 무대 위 4m에 떠 있던 문제 수정
  - `elevation` 기본값 `4.0` → `0` (프레임 하단이 무대 윗면 `h`에 착지)
  - `iz2 = h + elevation` — 무대 위에 서 있는 게이트 프레임 구조
  - 설치높이 UI 라벨: "설치 높이(지면 기준)" → "추가 높이(0 = 무대 위 착지)"

### 추가
- **창고 재고 제약**: `daeApplyTrussConfig`에서 재고 등록된 부품이 부족하면 적용 차단
  - 상하·좌우 쌍(×2)·코너 큐브 소요량을 재고와 대조
  - 부족 시 토스트로 부족 항목 안내 후 배치 취소

### 정리
- `emitBar` 죽은 좌표 계산 제거 (`originOff`/`rx`/`rz`/`pw` — 미사용)
  - 실제 사용되는 `rx_fb`/`rz_fb` 한 벌만 남겨 가독성 개선

---

## [2026-06-22] v0622-25 — 🔧 트러스 수직바 Z 위치 겹침 수정

### 수정
- 수직바 박스 translate Z 오류: `relZ + pc.offset` → `relZ + pc.offset + pc.length/2`
  - boxGeom은 원점 중심 기준이므로 중심점으로 배치해야 함
  - 이전: 각 조각이 length/2 만큼 아래로 내려가 하단 큐브와 겹침
  - 수정 후: 수직바 조각이 큐브 바로 위부터 정확히 배치됨

---

## [2026-06-22] v0622-24 — 📦 트러스 조각 박스 전용 + 임베드 원복

### 변경
- 트러스 조각 emitBar: 3D 모델 임베드 제거 → 박스 폴백 전용 (위치 계산 정확)
- `_embedDaeModel` 원복: translate/matrix 제거 코드 삭제 (모델 구조 파괴 방지)
- 박스로 정확한 직사각형 프레임 구조 보장 (큐브=0.22m, 바=실제 길이)

---

## [2026-06-22] v0622-23 — 📍 임베드 모델 위치 초기화

### 수정
- `_embedDaeModel()`: 스케치업이 저장한 모델 world position(translate/matrix) 제거
  - 스케치업 .dae는 모델 원래 위치를 파일에 내장 → 우리 위치 계산과 충돌
  - 각 임베드 노드의 최상위 translate/matrix를 제거해 원점(0,0,0) 기준으로 통일
- 결과: 트러스 조각들이 계산된 위치에 정확히 배치됨

---

## [2026-06-22] v0622-22 — 🔄 트러스 모델 회전 방향 수정

### 수정
- 트러스 .dae 모델이 Z축(수직) 기준으로 저장되어 있음을 반영
- 가로바: 회전 없음 → `<rotate>0 1 0 90</rotate>` (Z→X, 수평으로 눕힘)
- 세로바: `<rotate>0 1 0 -90</rotate>` 제거 → 회전 없음 (이미 Z축으로 올바름)

---

## [2026-06-22] v0622-21 — 🏗️ 트러스 수직 프레임 구조 수정

### 수정
- 트러스 프레임을 올바른 수직 직사각형 구조로 수정 (정면: 가로바 위/아래 + 세로바 좌/우)
- 세로바 축 오류 수정: Y 방향 → Z 방향(높이)으로 변경 (트러스가 세로로 세워진 형태)
- 세로바 큐브 중복 제거: `_buildTrussBar(withCubes=false)` — 큐브는 가로바 양끝 코너에만
- 설치높이 기준 수정: `iz2 = 무대높이(h) + elevation` (무대 바닥 기준 설치높이)
- 가로바 Y축: 3D 모델 회전 수정 (`<rotate>0 1 0 -90</rotate>` — 길이 방향을 Z축으로 정렬)
- 박스 폴백 수직 박스: Z 방향으로 pc.length 적용 (기존 Y 방향 오류 수정)

---

## [2026-06-22] v0622-20 — 🧱 트러스 조각 그룹핑 + 원점 설정

### 변경
- .dae 내보내기: 트러스 조각들을 부모 `<node>` 하나로 묶어 스케치업에서 클릭 한 번으로 전체 선택·이동 가능
- 조각별 translate를 부모 트러스 중심 기준 상대좌표로 변경 (절대→상대)
- 3D 모델 카드에 원점 위치 설정 추가: `중심(center)` / `시작점(start)`
  - center: 모델 중심이 원점 (기본, 직접 제작 모델)
  - start: 모델 한쪽 끝이 원점 (3D Warehouse 등 외부 모델)
- `lib3dSetOrigin(id, origin)` 함수 추가

---

## [2026-06-22] v0622-19 — ✅ 모델 항상 적용 + 트러스 조립 버그 수정

### 수정
- 3D 모델: 등록=항상 적용 (active 토글 개념 제거)
  - 토글 버튼 제거, 등록 현황 🟢=등록됨 / ⬜=미등록 단순화
  - 업로드 시 같은 타입 기존 파일 자동 교체 (타입당 1개 유지)
  - `lib3dGetActive()` → active 무관, 등록된 것 바로 반환
- 트러스 조립 버그: hTotalSpan/vTotalSpan이 0이면 조립 블록 진입 후 아무것도 안 그리고 박스도 건너뛰던 문제 수정 → 실제 span이 있을 때만 조립 실행

---

## [2026-06-22] v0622-18 — 🔍 .dae 치수 자동 추출 → 평면도·내보내기 자동 반영

### 변경
- 업로드 시 .dae 파일 바운딩박스 자동 계산 → rw/rd/rh 자동 저장
- 수동 치수 입력란 제거 (자동 추출로 대체, 카드에서 수정은 여전히 가능)
- 평면도 SVG 아이콘 크기: 등록된 모델의 실제 치수 적용 (rw=가로, rd=세로)
- 치수 감지 성공/실패 토스트 메시지 표시
- `lib3dExtractDims(fileText)` 함수 추가 (unitScale 자동 보정 포함)

---

## [2026-06-22] v0622-17 — 📐 3D 모델 실제 치수 입력 + 자동 활성화

### 수정
- 모델 업로드 시 자동으로 활성화 (기존 같은 타입은 비활성화)

### 추가
- 업로드 전 가로(W) × 세로(D) × 높이(H) 치수 입력란 추가
- 등록된 모델 카드에 치수 표시 및 수정 가능 (m 단위 3개 입력란)
- 치수 미입력 시 기본값 사용, 입력 시 .dae 내보내기 박스 폴백에 반영
- `lib3dSetDim()` 함수 추가

---

## [2026-06-22] v0622-16 — ✏️ 장비 이름 변경 + 커스텀 타입 직접 추가

### 변경
- 서브우퍼 → V SUB 이름 변경

### 추가
- 3D 라이브러리에서 장비 타입을 직접 추가 가능 (이름 입력 후 추가 버튼)
- 커스텀 타입은 등록 현황 패널 하단 "커스텀" 그룹에 표시
- 커스텀 타입 옆 ✕ 버튼으로 삭제 가능
- 커스텀 타입은 localStorage에 저장, 드롭다운에 자동 반영

---

## [2026-06-22] v0622-15 — 📋 3D 라이브러리 타입 확장 + 등록 현황 패널

### 추가
- 새 장비 타입 4종: 프론트모니터, 사이드필모니터, 딜레이스피커, 플랭카드
- 3D 라이브러리 드롭다운 카테고리 정리 (스피커류/콘솔·마이크/무대·구조물/트러스 부품)
- **등록 현황 패널**: 전체 타입을 카테고리별로 나열, 🟢적용중 / 🟡등록됨 / ⬜미등록 표시
- `LIB3D_GROUPS` 상수 추가 (카테고리 그룹 정의)

---

## [2026-06-22] v0622-14 — 🔧 트러스 3D 모델 라이브러리 연동

### 추가
- `LIB3D_TYPES`에 트러스 부품 5종 추가: 트러스 큐브(0.22m), 0.5m, 1m, 1.5m, 2m
- 3D 라이브러리 파일 선택 드롭다운에 "트러스 부품" 옵션그룹 추가
- `EQ_ICONS`에 트러스 타입 아이콘 추가
- 트러스 팝업 각 섹션 행에 3D 모델 등록 상태 뱃지 표시 (🟢3D / ⬜3D, 클릭 시 3D 라이브러리로 이동)
- `_buildTrussBar()` 헬퍼: 트러스 한 바의 조각 배열(큐브+부품) 생성
- `.dae 내보내기`에서 트러스를 조각별로 조립하여 출력 (상단H/하단H/좌측V/우측V 바)
- 3D 모델 미등록 시 박스 geometry 폴백 유지

---

## [2026-06-22] v0622-13 — 🖥️ 트러스 창고 재고 전광판 UI

### 개선
- 창고 재고를 전광판 스타일 대형 숫자 표시로 재설계
- +/- 누를 때마다 큰 숫자가 즉시 업데이트 (모노스페이스 36px)
- 재고 있으면 초록 테두리, 배치로 부족하면 빨간 테두리
- 하단에 "사용 N개" 표시 (0이면 숨김)
- 남은수량 텍스트 제거

---

## [2026-06-22] v0622-12 — 📍 트러스 부품 합계 위치 이동 (무대 평면도 바로 아래)

### 수정
- "배치된 트러스 부품 합계" 패널을 트러스 창고 재고 섹션 → 무대 평면도 바로 아래로 이동
- 트러스 배치 즉시 평면도 아래에서 확인 가능

---

## [2026-06-22] v0622-11 — 🔁 트러스 쌍 설치 ×2 명시 + 재고 계산 반영

### 수정/개선
- 가로 섹션 헤더: "상단 바 + 하단 바 × 2줄 쌍 설치" 문구 표시
- 세로 섹션 헤더: "좌측 바 + 우측 바 × 2줄 쌍 설치" 문구 표시
- 섹션 수량 재고 표시: "×2 = 실제 N개 / 남은 M개" 형식으로 변경
- `daeGetInventoryUsed()`: hSections·vSections 소비량을 ×2로 보정 (쌍 설치 반영)

---

## [2026-06-22] v0622-10 — 📊 트러스 배치 부품 합계 표시

### 추가
- 트러스 창고 재고 섹션 하단에 "배치된 트러스 부품 합계" 요약 자동 표시
- 배치된 트러스가 있을 때만 나타남 (0이면 숨김)
- 0.5m·1m·1.5m·2m 섹션 + 큐브 사용 개수 표시
- 재고 부족 항목은 빨간색 + ⚠ 경고

---

## [2026-06-22] v0622-09 — 🟡 트러스 큐브 고정 + 재고 차감 연동

### 수정
- **큐브 고정**: 트러스 팝업에서 큐브 수량 +/- 버튼 제거 → 꼭지점 위치(상좌·상우·하좌·하우) 항상 1개씩 고정
- **재고 차감 연동**: `daeRenderSVG()` 실행마다 `_updateInvRemainders()` 호출 → 트러스 배치/삭제 시 창고 재고 즉시 차감 반영
- 트러스 저장(적용) 시 큐브값 강제 1로 설정
- 트러스 창고 큐브 항목에 "사각프레임 1개당 4개 소비" 안내 표시

---

## [2026-06-22] v0622-08 — 🎭 무대계산기 PC 레이아웃 단일 스크롤 재구성

### 개선
- **서브탭 제거 (PC)**: 계산/3D도면 탭 숨김 → 단일 스크롤 페이지로 통합
- **배치 순서 재구성**: 무대크기 입력 → 배치다이어그램(전폭) → 계산결과(5열) → 클램프상세(4열) → 초기화/연동 버튼 → 무대크기+높이 → 무대평면도(전폭) → 행사장위치+.dae업로드(나란히) → 트러스창고 → 내보내기
- **계단 위치 선택** 삭제 (hidden input으로 대체)
- **배치 메모** 섹션 삭제
- 행사장 위치 + 3D 모델 라이브러리 PC에서 가로 2열 나란히 배치
- 무대 평면도 SVG 전폭 + 최소 높이 400px 확보
- 모바일에서는 기존 서브탭 방식 유지

---

## [2026-06-22] v0622-07 — 📱 헤더 모바일 수정 + 무대계산기 스타일 통일

### 수정
- 헤더 버튼 모바일에서 줄바꿈 오버플로 해결 (`flex-wrap: nowrap`)
- 모바일(767px 이하)에서 ☁️·🔍 버튼 숨김 처리
- 무대계산기 CSS: 하드코딩된 색상/반경 → 앱 CSS 변수(`--radius`, `--accent`, `--accent2` 등)로 통일
- 무대계산기 스타일이 다크/라이트 테마 전환에 정상 대응

---

## [2026-06-22] v0622-06 — 🔤 로고 폰트 수정

### 수정
- 로고 "한바다일지" 폰트 JetBrains Mono → Noto Sans KR 변경
- 영어 코딩 폰트로 한글 렌더링 시 글자마다 네모 테두리처럼 보이던 현상 해결
- letter-spacing 3px → 1px 조정

---

## [2026-06-22] v0622-05 — 🎭 무대계산기 탭 구조 개선

### 개선
- **계산 탭**: 탭 안에서 좌우 분리 제거 → 전폭 단일 레이아웃
  - 무대크기 입력 + 배치 다이어그램 나란히 (상단)
  - 계산결과 + 클램프 상세 나란히 (하단)
  - 초기화 / 3D도면연동 버튼 하단
- **3D 도면 탭**: 컨트롤(280px) + 평면도(전폭) 유지 — 평면도가 메인이므로
- 계산→3D도면 순서로 사용하는 흐름에 맞게 탭 분리

---

## [2026-06-22] v0622-04 — 🎭 무대계산기 계산탭 전면 재구성

### 수정
- **구조적 버그 수정**: `#sc-diagramCard`가 `.sc-calc-left` 안에 갇혀 오른쪽 영역 전체 공백 발생 → 올바른 위치로 이동
- **캔버스 크기 수정**: 부모 전폭 → 실제 무대 내용 폭에 맞게 자동 조정, 빈 배경 공간 제거
- **계산결과 가로 5열 배치**: 상판/지지대/X대/클램프합계/무대면적 한 줄로
- **클램프 상세 가로 4열 배치**: 1개/2개/4개/합계 그리드형으로 변경
- 캔버스 셀 최대 크기 36→48px, 내용 중앙 정렬

---

## [2026-06-22] v0622-03 — 🔧 무대계산기 계산탭 좌측 빈 공간 제거

### 수정
- 계산탭 좌측 카드들을 `.sc-calc-left` 래퍼로 묶어 grid-row 스패닝 방식 제거
- 다이어그램이 길어져도 좌측에 빈 공간 생기지 않음
- `#sc-diagramCard` grid-row 불필요 속성 제거, sticky만 유지

---

## [2026-06-22] v0622-02 — 🎭 무대계산기 PC 레이아웃 전면 개선

### 개선
- **앱 전체 최대 너비** 1280px → 1440px (와이드 모니터 여백 감소)
- **무대계산기 컨테이너** max-width 제한 해제, 패딩 축소 (28px→24px)
- **계산 탭** 그리드: `5fr 6fr` → `360px 1fr` — 컨트롤 고정, 다이어그램 나머지 전폭 사용
- **배치 다이어그램** sticky 적용 — 스크롤해도 항상 보임
- **3D 도면 탭** 그리드: `4fr 5fr` → `300px 1fr` — 무대 평면도 대폭 확장
- **1400px+ 화면** 행사일지 4열, 업무 4열, 장비/알바 6열 그리드 추가

---

## [2026-06-22] v0622-01 — 🖥️ 행사일지 폼 PC 레이아웃 최적화

### 개선
- **행사일지 폼 2컬럼 그리드 (PC)** — 세로 나열 → 좌우 2열 배치로 화면 공간 활용
  - 기본정보·피드백&메모 섹션은 전폭 유지
  - 인력배치 ↔ 타임라인, 음향장비세팅 ↔ 모니터스피커, 그외투입장비 ↔ 무대설치, 전기 ↔ 이슈&해결 나란히 배치
- **콘솔/스피커 브랜드 선택 그리드** — 2열 → 3열 확장 (PC)
- 모바일은 기존 세로 레이아웃 유지

---

## [2026-06-09] — 🧱 레고 드래그앤드롭 조립 시스템 (세션 4)

### 추가
- **[＋] FAB 버튼** — 보드 우하단 고정, 누르면 부품 선택 시트 슬라이드업
- **부품 선택 시트** — `#pageStage` 내부 `position:absolute`, 5종 부품 카드 (SKP 썸네일 + 이모지 폴백)
- **`legoAddToCenter()`** — 부품 탭 시 보드 중앙 배치, 겹침 방지 오프셋
- **자동 스냅** — 36px 이내 연결점 접근 시 초록 원 표시 + 흡착
- **3D 동기화** — 🎲 버튼으로 토글, 보드 배치 내용 3D로 반영

### 수정
- `display:flex !important` → `#pageStage.active` — 다른 탭에서 구조물 설계 노출 버그 수정
- 부품 시트 `body` → `#pageStage` 내부로 이동 — 타 페이지 침범 방지
- 구버전 `.stage-3d-wrap { height:260px }` CSS 제거 — 3D 기본 숨김 정상 동작

---

## [2026-06-09] — 📐 자재규격 설정 신설 + 🧱 조립 도구 스펙 연동

### 추가
- **📐 자재규격 버튼** — 무대설치 탭 상단에 상시 표시
  - H프레임 / X대 / 수평연결봉 / 트러스빔 / LED프레임 / 발판 6가지 카테고리
  - 현장 실측값(mm) 입력 → 3D 모델 자동 반영
  - 규격 추가 / 수정 / 삭제, localStorage 저장
- **조립 도구 스펙 연동**
  - 비계틀 블록: H프레임 규격 선택 → 베이 폭/단 높이 자동 적용
  - 트러스 빔 블록: 트러스 규격 선택 → 길이/단면 자동 적용
  - LED 프레임 블록: LED 프레임 규격 선택 → 가로/세로/단면 자동 적용

---

## [2026-06-09] — 🧱 조립 도구(Assembly Tool) 신설

### 추가
- 비계틀 · 수평트러스 · 수직트러스 · LED 블록 자유 조합
- 블록별 치수 실시간 조절, 3D 즉시 반영

---

## [2026-06-09] — 🏟️ 이벤트 구조물 타입 신설 (고성 아이언맨 구조물)

### 추가
- **🏟️ 이벤트 구조물 카테고리** — 비계틀 + 사각 트러스 + LED 프레임 복합 구조
  - `고성 아이언맨 구조물 1.skp` 분석 후 Three.js로 재현
  - **비계틀 구간**: 베이×단×열 수, 단 높이 조정 → H프레임+X대+깊이 연결봉 실시간 렌더
  - **사각 트러스 빔**: 4코너 코드 + 면 대각 가새 → 황금색 사각 트러스 (단면 크기 조정 가능)
  - **수평 트러스 빔**: 비계틀 상단 전·후면 + 좌우 끝단 연결
  - **좌측 수직 트러스 프레임**: Π형 포털 프레임 (앞뒤 수직 기둥 + 상단 빔)
  - **LED 스크린 프레임**: 우측 독립 사각 트러스 프레임 + 반투명 블루 패널
  - 모든 치수 실시간 조정: 베이 수, 단 수, 열 수, LED 크기, 트러스 단면, 좌측 프레임 on/off
- **이벤트 타입 모달 버튼** 추가 (🏟️ 이벤트)

---

## [2026-06-09] — 🏗️ 무대설치 탭 신설 (Three.js 3D 뷰어)

### 추가
- **🏗️ 무대설치 탭 신설** — 구조물 3D 설계 도구
  - **서브 카테고리**: 무대설치 / 비계틀설치 / ＋ (직접 추가)
  - **Three.js 3D 뷰어**: 마우스 드래그 회전, 스크롤 확대축소, 터치 핀치줌
  - **무대설치**: 가로·세로·높이·단 수 입력 → 박스형 3D 무대 실시간 생성
  - **비계틀설치**: 베이 수·단 수·깊이·프레임 폭·단 높이·X대 여부 → 파이프 3D 비계틀 실시간 생성
  - **자재 수량 자동 계산**: 면적, 합판 장 수, H프레임 수, X대 수 등
  - **카테고리 추가·수정·삭제** (기본 2개는 잠금, 커스텀 항목만 수정·삭제 가능)
  - **치수 변경 즉시 반영**: 입력값 바꾸면 3D + 수량 자동 갱신

---

## [2026-06-09] — 장비 사진 UI 전면 개편 + 장비사진 업로드

### 추가
- **장비사진 17개** GitHub Pages 정적 호스팅 (`images/equipment/`)
- **14개 장비 사진 자동 매핑** (메인스피커·우퍼·4채널앰프·모니터스피커·메인콘솔·스테이지박스·무선마이크수신기·무선바디팩·콘덴서마이크·유선마이크·안테나분배기·광대역안테나·DI BOX·이동용앰프)

### 변경
- **팝업 레이아웃**: 기본정보(좌 3/4) + 사진(우 1/4) 가로 배치
- **사진 슬롯**: 4칸 → 1칸으로 축소
- **장비 카드**: 사진 썸네일 표시, 없으면 📷 + 버튼으로 바로 촬영/업로드 가능

---

## [2026-06-08] — 장비관리대장 음향장비 소분류 기능 추가

### 추가
- **음향장비 소분류 탭** — 음향장비 카테고리 선택 시 소분류 필터 바 자동 표시
  - 기본 6개 소분류: 스피커류 / 앰프류 / 프로세서·이퀄라이저 / 콘솔·믹서 / 마이크류 / 케이블·기타
  - `전체` 선택 시 소분류별 헤더로 그룹핑하여 표시
  - 특정 소분류 선택 시 해당 장비만 필터링
- **⚙️ 소분류 관리 모달**
  - 소분류 추가 / 이름 수정 / 삭제
  - 소분류 삭제 시 해당 장비의 소분류 자동 초기화, 보유 장비 수 경고
  - 수정 시 기존 장비들의 subcat 일괄 변경
- **장비 추가·수정 폼** — 음향장비 카테고리 선택 시 소분류 드롭다운 표시
- **장비 상세 보기** — 소분류 필드 표시
- **기존 데이터 자동 마이그레이션** — 소분류 없는 기존 음향장비 항목에 이름 기반으로 소분류 자동 배정

---

## [2026-06-05] — 알바관리 신설 / 장비카테고리 개편 / 계정·비밀번호 변경

### 변경
- **로그인 계정**: admin/6293 → hanbada/2375
- **모든 수정·삭제 암호**: 6293 → 2375
- **장비관리 카테고리**: 이벤트장비 제거 → 사무실장비·조명장비·특효장비·기타장비 추가

### 추가
- **대표지시 탭 드롭다운**: 클릭 시 "대표지시 / 알바관리" 선택 메뉴
- **👷 알바관리 페이지 신설**
  - 알바 명단 카드 (이름·연락처·역할)
  - 스케줄 탭 (월별 스케줄 목록)
  - 알바 클릭 → 상세 팝업 (기본정보 + 스케줄 이력 + 추가)
  - 수정·삭제 암호 잠금 (2375)
  - Supabase `id='alba'` 클라우드 동기화

---

## [2026-06-05] — 장비관리대장 독립 페이지 신설

**커밋**: `(pending)`

### 추가
- **🗂️ 장비관리대장 탭 신설** (기존 체크리스트와 완전 분리)
  - 음향장비 / 무대장비 / 이벤트장비 3개 카테고리
  - 전체 / 사용중 / 미사용 필터
  - 장비 카드 그리드 (사진·관리내역 건수 표시)
- **장비 상세 팝업 카드**: 장비명, 모델, 수량, 납품업체, 시리얼번호, 담당자, 연락처, 상태
- **사진 업로드** (최대 4장, 800px 리사이즈, JPEG 0.7 압축, 클릭 시 확대 라이트박스)
- **관리내역 로그**: 날짜/내용/업체 추가, 삭제 (암호 6293)
- **수정/삭제 암호 잠금** (6293) — 기본정보 수정, 장비 삭제, 관리내역 삭제
- **장비 추가** 기능 (카테고리 선택 포함)
- PDF 3개 기반 기본 장비 57종 자동 탑재 (최초 1회)
- Supabase `vehicle_data id='equipment'` 클라우드 동기화
- Realtime WebSocket으로 다기기 실시간 반영

---

## [2026-06-04] — 체크리스트 장비관리대장 카테고리 추가

**커밋**: `(pending)`

### 추가
- **장비관리대장 카테고리 신설**
  - PDF 3개(음향장비 관리 카드, 이벤트 장비 관리대장, 이벤트 음향 장비 관리대장) 통합 정리
  - 섹션 헤더(`【 ... 】`) 아이템 지원 — 체크 불가, 구분선 역할
  - 6개 섹션: 스피커/앰프, 콘솔/믹서, 마이크류, 케이블/기자재, 무대장비, 특효/기타
  - 총 55개 항목 기본 탑재 (`JANGBI_DEFAULT`)
  - 카테고리 비어 있을 때 자동으로 기본값 채움 (`initJangbiDefault()`)
- **진행률 계산에서 섹션 헤더 제외** — `updateChecklistProgress()` 개선

---

## [2026-05-28] — 3.5톤 차량 데이터 저장 버그 수정

**커밋**: `(pending)`

### 수정
- **3.5톤 마이티 데이터 클라우드 저장 안 되는 문제 수정**
  - Supabase `vehicle_data` 테이블의 `v35` 컬럼 의존성 제거
  - v15 + v35 데이터를 `v15` 컬럼 하나에 합쳐서 저장 (`_format: 'combined'` 마커)
  - 구버전 데이터 하위 호환 유지
- **정비 기록 저장 시 자동 클라우드 동기화 추가**
  - 기존: 정비 기록 저장이 로컬(localStorage)에만 저장됨
  - 수정: `saveMaintModal()` / `deleteMaintenance()` 호출 시 `_syncVehicleCloud()` 자동 실행
  - 토스트 메시지에 ☁️ 아이콘 추가
- `_syncVehicleCloud()` — 조용한 백그라운드 클라우드 동기화 함수 신설
- `_applyVehicleCloudData()` — 클라우드 데이터 적용 (combined/legacy 둘 다 지원)

---

## [2026-05-26] — 행사일지 폼 UI 대대적 개편

**커밋**: `ec08104`

### 수정
- **콘솔/스피커 입력 간소화**
  - 모델 그리드 제거 → 브랜드 선택 후 텍스트 입력칸 1개만 표시
  - `f_consoleModel` / `f_speakerModel` 단일 텍스트 필드로 저장
- **섹션 순서 변경**
  - 모니터 스피커 세팅 → 투입 장비 앞으로 이동
  - 전기 → 이슈&해결 앞으로 이동
- **내부 메모 텍스트 영역 3배 확장** (`min-height: 246px`)
- **사진 업로드 기능 추가** (4장씩)
  - 음향장비 섹션에 사진 4장 업로드 그리드 추가
  - 무대설치 섹션에 사진 4장 업로드 그리드 추가
  - Base64 저장, 최대 800px 리사이즈, JPEG 0.7 압축
  - `soundPhotos` / `stagePhotos` 배열로 일지 데이터에 저장

---

## [2026-05-26] — 멀티기기 동기화 3가지 수정

**커밋**: `472b5ff`

### 수정
- **사용자 계정 클라우드 동기화**
  - `syncUsersToCloud()` / `loadUsersFromCloud()` 함수 추가
  - Supabase `vehicle_data` 테이블의 `id='sl_users'` 행에 사용자 배열 저장 (신규 테이블 불필요)
  - `doLogin`: 로컬 미발견 시 자동으로 클라우드 조회 후 재시도 (다른 기기에서 로그인 가능)
  - `doRegister` / `deleteUser`: 변경 시 즉시 클라우드 반영
  - `autoLoadAll`: 로그인 직후 사용자 목록 최신화
  - `initRealtime`: `sl_users` 행 변경 실시간 수신

- **차량 데이터 Realtime 구독 추가**
  - `vehicle_data` 테이블을 WebSocket 구독 목록에 추가
  - 다른 기기에서 차량 정보 저장 시 수동 새로고침 없이 즉시 반영

- **업무 저장 API 최적화**
  - `syncOneTaskToCloud(task, type)` 함수 추가
  - 업무 1개 변경(저장·상태변경) 시 해당 건만 Supabase upsert
  - `saveTasksLocal()`에서 전체 업무 일괄 업로드 제거

---

## [이전] — 초기 구축

- 행사일지, 창고/사무실/대표지시 업무, 차량 관리 기능
- Supabase 연동 (soundlogs, tasks, vehicle_data, access_logs)
- 로그인/회원가입 시스템 (관리자 승인 코드: 6293)
- WebSocket Realtime (soundlogs, tasks)
- 다크/라이트/파스텔 테마 선택
- GitHub Pages 배포
